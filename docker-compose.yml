services:
  app:
    build: .
    container_name: visagely
    restart: unless-stopped
    ports:
      - '3004:3000'
    environment:
      ORIGIN: ${ORIGIN:-https://visagely.com}
      NODE_ENV: production
